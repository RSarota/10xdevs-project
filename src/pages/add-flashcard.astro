---
import AppLayout from "../layouts/AppLayout.astro";
import AddFlashcardPage from "../components/AddFlashcardPage";

export const prerender = false;

// Additional security check (middleware already protects, but explicit is better)
const { user } = Astro.locals;
if (!user) {
  return Astro.redirect("/auth/login");
}
---

<AppLayout title="Dodaj fiszkę - 10xCards" currentPath="/add-flashcard">
  <AddFlashcardPage client:load />
</AppLayout>
